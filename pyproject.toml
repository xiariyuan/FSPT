[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "fspt"
version = "0.0.0"
requires-python = ">=3.10"
description = "FSPT foundation package for point tracking research"

[tool.setuptools.packages.find]
include = ["fspt*"]
